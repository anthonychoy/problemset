{
  "name": "problemset",
  "version": "1.0.0",
  "dependencies": {
    "rand-token": "^0.2.1"
  },
  "devDependencies": {
    "mocha": "^2.4.5"
  },
  "scripts": {
    "test": "mocha --recursive --timeout 60000 test/"
  }
}
